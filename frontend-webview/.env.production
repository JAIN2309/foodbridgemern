# ── Production env vars for frontend-webview ──
# These are embedded at build time by Vite (import.meta.env.VITE_*)
# Update the URL below after you deploy the backend to Render.

VITE_API_URL=https://foodbridge-api.onrender.com/api
VITE_SOCKET_URL=https://foodbridge-api.onrender.com
